## Parent

Part of #25 (Spec: Apex Pilot desktop UI overhaul — Mission Control).

## What to build

Soon after Spec shell IA, Ctrl+Shift+P opens a minimal command palette that discovers and runs shell actions already available (panel toggles and common toolbar/menu commands). Configurable shortcuts remain Future.

## Acceptance criteria

- [ ] Ctrl+Shift+P opens a minimal command palette
- [ ] Palette lists and runs existing shell actions (at least panel toggles and common menu/toolbar commands)
- [ ] Vitest covers palette open and invoking a known shell action

## Blocked by

- PLACEHOLDER_BLOCKERS
